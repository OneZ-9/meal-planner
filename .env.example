MONGODB_URI=
AUTH_SECRET=
AUTH_URL=http://localhost:3000
# Vercel Blob (recipe image upload) — create a Blob store under Vercel
# dashboard > Storage, or run `vercel env pull` once the project is linked.
BLOB_READ_WRITE_TOKEN=
